package com.generationxray.config.handler;

import com.generationxray.config.GenerationXrayConfig;
import me.shedaniel.autoconfig.ConfigHolder;

public class TestCommandHandler extends AbstractConfigHandler {
    public TestCommandHandler(ConfigHolder<GenerationXrayConfig> configHolder) {
        super(configHolder);
    }
    
    
}
